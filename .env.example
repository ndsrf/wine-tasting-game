# ===========================================
# PRODUCTION DEPLOYMENT CONFIGURATION
# ===========================================

# ---------------------------------------
# Database Configuration
# ---------------------------------------
# For Docker Compose: postgresql://wineuser:your-secure-password@postgres:5432/wine_tasting_db
# For external DB: Use your managed PostgreSQL connection string
DATABASE_URL="postgresql://wineuser:your-secure-password@postgres:5432/wine_tasting_db"

# PostgreSQL container settings (Docker Compose only)
POSTGRES_USER="wineuser"
POSTGRES_PASSWORD="your-secure-password"
POSTGRES_DB="wine_tasting_db"
POSTGRES_PORT="5432"

# ---------------------------------------
# Authentication & Security
# ---------------------------------------
# Generate with: openssl rand -hex 32
NEXTAUTH_SECRET="generate-a-secure-random-32-char-string"
JWT_SECRET="generate-another-secure-random-32-char-string"

# Your public URL (e.g., https://wine.example.com or http://your-server-ip:3000)
NEXTAUTH_URL="http://localhost:3000"

# Allowed origins for CORS (comma-separated, for Cloudflare tunnels or reverse proxies)
# Example: "https://wine.example.com,https://wine-staging.example.com"
# Leave empty to allow all origins in production (less secure)
ALLOWED_ORIGINS=""

# ---------------------------------------
# Google OAuth (Optional)
# ---------------------------------------
# Leave empty if not using Google login
# Get from: https://console.cloud.google.com/apis/credentials
# Make sure to add your domain to "Authorized JavaScript origins"
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
NEXT_PUBLIC_GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"

# ---------------------------------------
# LLM Provider Configuration
# ---------------------------------------
# Choose LLM provider: "openai" (default), "gemini", or "anthropic"
LLM_PROVIDER="openai"

# OpenAI Configuration
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key-here"
# Model to use (default: gpt-4)
# Options: gpt-4, gpt-4-turbo-preview, gpt-3.5-turbo
OPENAI_MODEL="gpt-4"

# Google Gemini Configuration (only needed if LLM_PROVIDER="gemini")
# Get API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"
# Model to use (default: gemini-1.5-pro)
# Options: gemini-1.5-pro, gemini-1.5-flash, gemini-pro
GEMINI_MODEL="gemini-1.5-pro"

# Anthropic Configuration (only needed if LLM_PROVIDER="anthropic")
# Get API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="sk-ant-your-anthropic-api-key-here"
# Model to use (default: claude-3-5-sonnet-20241022)
# Options: claude-3-5-sonnet-20241022, claude-3-opus-20240229, claude-3-sonnet-20240229, claude-3-haiku-20240307
ANTHROPIC_MODEL="claude-3-5-sonnet-20241022"

# ---------------------------------------
# Redis Configuration
# ---------------------------------------
# For Docker Compose: redis://:your-redis-password@redis:6379
# For external Redis: Use your Redis connection string
REDIS_URL="redis://:your-redis-password@redis:6379"
REDIS_PASSWORD="your-redis-password"
REDIS_PORT="6379"

# ---------------------------------------
# Application Settings
# ---------------------------------------
NODE_ENV="production"
APP_PORT="3000"

# ---------------------------------------
# GitHub Container Registry (for deployment)
# ---------------------------------------
# Format: owner/repository (e.g., yourusername/wine-tasting-game)
GITHUB_REPOSITORY="ndsrf/wine-tasting-game"